import {LinkedList  } from "dsa.js";
import Node from "dsa.js/src/data-structures/linked-lists/node";
// const { LinkedList } = require("dsa.js");


let arr = [2, 10, 7];
let list = new LinkedList();

let  head = Node(arr[0]);
for (let i = 0; i < arr.length; i++) {
   const element = arr[i];
   let temp =  Node()
   
}
console.log(list.length);
